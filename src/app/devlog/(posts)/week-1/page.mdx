# Week 1: Breaking Ground

This week I focused on the core architecture of the desktop app. I decided to go with **Electron** because of its popularity.

## Key Technical Decisions
- **Database:** SQLite for local-first persistence.
- **State:** Jotai for atomic state management.

```javascript
// This will be automatically highlighted if you added a syntax highlighter
const app = await init();